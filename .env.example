# =============================================================================
# artoo environment configuration
# Copy this file to .env and fill in your values.
# =============================================================================

# -----------------------------------------------------------------------------
# Minimal setup — single model for all roles
# Any OpenAI-compatible endpoint works (OpenAI, DeepSeek, Qwen, Kimi, etc.)
# -----------------------------------------------------------------------------
OPENAI_API_KEY="your-api-key-here"
OPENAI_BASE_URL="https://api.openai.com"
OPENAI_MODEL="gpt-4o"

# -----------------------------------------------------------------------------
# Optional: two-tier model split
#
# BRAIN tier — reasoning-capable model for:
#   R1 Perceiver, R2 Planner, R4b MetaValidator
#
# TOOL tier — fast execution model for:
#   R3 Executor, R4a AgentValidator
#
# Any unset tier variable falls back to the shared OPENAI_* value above.
# -----------------------------------------------------------------------------
#BRAIN_API_KEY="your-brain-api-key"
#BRAIN_BASE_URL="https://api.deepseek.com"
#BRAIN_MODEL="deepseek-reasoner"

#TOOL_API_KEY="your-tool-api-key"
#TOOL_BASE_URL="https://api.deepseek.com"
#TOOL_MODEL="deepseek-chat"

# Enable extended thinking for the brain model (set to "true" if supported).
# Tested with DeepSeek-R1 / Claude 3.5+ extended thinking endpoints.
#BRAIN_ENABLE_THINKING="false"

# -----------------------------------------------------------------------------
# Web search tool
#
# The `search` tool uses DuckDuckGo by default (no API key required).
# It is always available in the Executor's toolbox.
# -----------------------------------------------------------------------------
